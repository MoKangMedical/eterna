
# 念念人情味增强系统集成指南

## 概述

我已经为念念项目创建了6个增强系统，让念念的对话更加有人情味：

1. **增强的情感感知系统** (`emotion_analysis.py`)
2. **增强的记忆系统** (`memory_system.py`)
3. **丰富的人格建模系统** (`personality_system.py`)
4. **智能的主动关怀系统** (`proactive_care_system.py`)
5. **对话自然度提升系统** (`dialogue_naturalness.py`)
6. **情感表达细节系统** (`emotional_expression.py`)

## 集成步骤

### 1. 文件放置

将以下文件放置到 `api/` 目录下：
- `emotion_analysis.py`
- `memory_system.py`
- `personality_system.py`
- `proactive_care_system.py`
- `dialogue_naturalness.py`
- `emotional_expression.py`

### 2. 修改现有的 `app.py`

在 `app.py` 中添加以下导入：

```python
# 导入新增的系统
from api.emotion_analysis import EnhancedEmotionAnalyzer, EmotionAnalysis
from api.memory_system import EnhancedMemorySystem, MemoryContext
from api.personality_system import EnhancedPersonalityModeling, PersonalityProfile
from api.proactive_care_system import EnhancedProactiveCareSystem, CarePlan
from api.dialogue_naturalness import NaturalDialogueSystem, DialogueState, DialogueContext
from api.emotional_expression import EmotionalExpressionSystem

# 初始化各个系统
emotion_analyzer = EnhancedEmotionAnalyzer()
memory_system = EnhancedMemorySystem()
personality_modeling = EnhancedPersonalityModeling()
proactive_care = EnhancedProactiveCareSystem()
dialogue_naturalness = NaturalDialogueSystem()
emotional_expression = EmotionalExpressionSystem()
```

### 3. 修改聊天API

在现有的 `chat_with_loved_one` 函数中，替换情感分析和回应生成部分：

```python
@app.post("/api/chat", response_model=ChatResponse)
async def chat_with_loved_one(
    msg: ChatMessage,
    request: Request,
    current_user: dict = Depends(get_current_user),
):
    # ... 现有的验证代码 ...
    
    # 1. 使用增强的情感分析
    emotion_analysis = emotion_analyzer.analyze_emotion(msg.message)
    detected_emotion = emotion_analysis.primary_emotion
    emotion_intensity = emotion_analysis.intensity.value / 5.0  # 转换为0-1范围
    
    # 2. 使用增强的记忆系统
    memory_rows = conn.execute(
        "SELECT content FROM memories WHERE loved_one_id = ? ORDER BY created_at DESC LIMIT 20",
        (msg.loved_one_id,),
    ).fetchall()
    all_memories = [{"content": row["content"]} for row in memory_rows]
    
    memory_context = memory_system.select_relevant_memories(
        current_message=msg.message,
        current_emotion=detected_emotion,
        all_memories=all_memories,
        conversation_history=[],  # 可以从数据库加载历史对话
        limit=3
    )
    
    # 3. 使用丰富的人格建模
    personality_traits = loved_one.get("personality_traits", {})
    personality_profile = personality_modeling.build_personality_profile(
        name=loved_one["name"],
        relationship=loved_one.get("relationship", "亲人"),
        personality_traits_dict=personality_traits,
        speaking_style=loved_one.get("speaking_style", "温柔亲切"),
        additional_info=loved_one.get("additional_info")
    )
    
    # 4. 生成基础回应（使用现有的MIMO API或回退回应）
    if MIMO_API_KEY:
        try:
            # 构建增强的提示
            enhanced_prompt = personality_modeling.generate_personality_prompt(personality_profile)
            
            # 添加记忆上下文
            if memory_context.relevant_memories:
                memory_text = "\n".join([f"- {mem.content}" for mem in memory_context.relevant_memories])
                enhanced_prompt += f"\n\n相关记忆：\n{memory_text}"
            
            # 添加情感分析结果
            enhanced_prompt += f"\n\n用户当前情感：{detected_emotion}（强度：{emotion_intensity:.1f}）"
            enhanced_prompt += f"\n建议回应风格：{emotion_analysis.suggested_response_style}"
            
            ai_response = await generate_text_response_with_mimo(
                loved_one=loved_one,
                user_message=msg.message,
                emotion=detected_emotion,
                memory_context=memory_text if memory_context.relevant_memories else "",
                request=request,
                mode=interaction_mode,
                intensity=msg.intensity,
            )
        except Exception:
            ai_response = build_fallback_response(
                loved_one=loved_one,
                user_message=msg.message,
                emotion=detected_emotion,
                memory_context="",
                intensity=msg.intensity,
            )
    else:
        ai_response = build_fallback_response(
            loved_one=loved_one,
            user_message=msg.message,
            emotion=detected_emotion,
            memory_context="",
            intensity=msg.intensity,
        )
    
    # 5. 使用对话自然度系统调整回应
    dialogue_context = DialogueContext(
        current_state=DialogueState.DEEP_CONVERSATION,  # 可以根据实际情况调整
        state_turns=1,  # 可以从数据库加载
        conversation_history=[],  # 可以从数据库加载
        user_intent="general_chat",  # 可以从情感分析中获取
        emotional_tone=detected_emotion,
        topics_discussed=[],
        memory_references=[mem.content[:20] for mem in memory_context.relevant_memories],
        last_response_time=datetime.now()
    )
    
    natural_response = dialogue_naturalness.generate_natural_response(
        dialogue_context=dialogue_context,
        ai_response=ai_response,
        user_emotion=detected_emotion,
        personality_profile=personality_profile
    )
    
    # 6. 使用情感表达系统增强回应
    enhanced_response = emotional_expression.add_emotional_expressions(
        text=natural_response,
        emotion=detected_emotion,
        intensity=emotion_intensity,
        personality_traits=personality_traits,
        context={}
    )
    
    # ... 现有的音频和视频生成代码 ...
    
    return ChatResponse(
        loved_one_id=msg.loved_one_id,
        loved_one_name=loved_one["name"],
        response_text=enhanced_response,  # 使用增强后的回应
        response_audio_url=response_audio_url,
        response_video_url=response_video_url,
        interaction_mode=interaction_mode,
        mode_note=video_mode_note or build_mode_note(requested_mode, available_modes),
        available_modes=available_modes,
        emotion_detected=detected_emotion,
        memory_triggered=memory_context.relevant_memories[0].content[:100] if memory_context.relevant_memories else None,
        memory_refs=[mem.content[:50] for mem in memory_context.relevant_memories[:3]],
    )
```

### 4. 修改主动关怀系统

在现有的主动关怀功能中，集成新的关怀系统：

```python
@app.get("/api/proactive/feed")
async def get_proactive_feed(current_user: dict = Depends(get_current_user)):
    # ... 现有的代码 ...
    
    # 使用新的主动关怀系统
    care_plan = proactive_care.analyze_care_opportunities(
        user_profile=current_user,
        usage_history=usage_history,  # 从数据库加载
        emotional_history=emotional_history,  # 从数据库加载
        current_context={
            "current_date": datetime.now(),
            "weather": get_weather_info()  # 可以从天气API获取
        }
    )
    
    # 生成关怀消息
    care_messages = []
    for opportunity in care_plan.opportunities[:3]:  # 只返回前3个
        message = proactive_care.generate_care_message(opportunity)
        care_messages.append({
            "trigger": opportunity.trigger.value,
            "message": message,
            "priority": opportunity.priority,
            "suggested_time": opportunity.trigger_time.isoformat()
        })
    
    return {"care_messages": care_messages}
```

### 5. 数据库更新

可能需要更新数据库模式以支持新功能：

```sql
-- 添加情感分析记录表
CREATE TABLE IF NOT EXISTS emotion_analysis_logs (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    loved_one_id TEXT NOT NULL,
    user_message TEXT NOT NULL,
    detected_emotion TEXT NOT NULL,
    emotion_intensity REAL,
    analysis_result TEXT,  -- JSON格式存储完整分析结果
    created_at TEXT NOT NULL
);

-- 添加对话状态记录表
CREATE TABLE IF NOT EXISTS dialogue_states (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    loved_one_id TEXT NOT NULL,
    current_state TEXT NOT NULL,
    state_turns INTEGER DEFAULT 0,
    conversation_context TEXT,  -- JSON格式存储对话上下文
    updated_at TEXT NOT NULL
);

-- 添加主动关怀计划表
CREATE TABLE IF NOT EXISTS proactive_care_plans (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    care_plan TEXT NOT NULL,  -- JSON格式存储关怀计划
    scheduled_time TEXT NOT NULL,
    status TEXT DEFAULT 'pending',
    created_at TEXT NOT NULL
);
```

## 测试建议

1. **单元测试**：为每个新系统编写单元测试
2. **集成测试**：测试整个对话流程
3. **用户测试**：让真实用户测试对话体验
4. **性能测试**：确保新系统不会影响响应速度

## 注意事项

1. **向后兼容**：确保新系统不会破坏现有功能
2. **性能优化**：新系统可能增加计算开销，需要优化
3. **数据隐私**：确保情感分析和记忆系统符合隐私政策
4. **错误处理**：为新系统添加适当的错误处理和回退机制

## 后续优化方向

1. **机器学习集成**：使用机器学习模型改进情感分析
2. **个性化学习**：根据用户反馈优化人格建模
3. **多模态增强**：结合语音和视频的情感分析
4. **实时适应**：根据对话实时调整情感表达

---

*念念 Eterna - 让AI更有温度*
*集成指南 v1.0*
